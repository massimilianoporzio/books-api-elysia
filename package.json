{
  "name": "books-api",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch api/index.ts",
    "vercel-build": "echo Hello",
    "build": "bun build api/index.ts --outdir=dist --target=node"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
